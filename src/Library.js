function createLibrary(lib) {
    var denvLib = {
    name: lib,
    shelves: ["fantasy", "fiction", "nonFiction"]
  };
  console.log(denvLib.name);
  console.log(denvLib.shelves);
}

function addBooks(lib, theme) {
  for (var i = 0; i = shelves.length; )
}

module.exports = {
  createLibrary,
  addBook,
  // checkoutBook
};
